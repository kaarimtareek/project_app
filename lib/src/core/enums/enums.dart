enum LoginState { logined, notLogined }
enum ThemeState { dark, light }
enum StoriesState { loading, idle, error }
enum CommentsState { loading, idle, error }
enum AuthenticationState { idle, busy, error }
